import { RegularExpressionValidatorDirective } from './regular-expression-validator.directive';

describe('RegularExpressionValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new RegularExpressionValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
